---
name: 3d-structure-review
description: 使用本机3D/CAD软件，结合配置表对STEP/STP或原生装配做结构与开模前评审，按需开展注塑风险、受力仿真和可靠性分析，输出带定位证据的可编辑报告。适用于3D结构图档评审、开模评审、结构DFM及注塑专项；不用于仅生成外观概念图。
---

# 3D结构图档评审

把需求、真实 CAD 几何、工程判断和关闭证据连起来。默认中文交付，优先保留可编辑报告、原生审阅副本、原始测量与图片。收到“评审”请求时，只创建分析和交付文件，不直接改产品设计。

## 本轮范围

从当前请求和已确认上下文确定图档版本、配置来源、评审目的、交付位置和分析模块。后续补充是范围调整；“跳过受力、只做注塑”立即停止相应分支并保留已完成证据，不把它变成下一次任务的永久默认。

| 用户任务 | 执行模块 | 需要时读取 |
|---|---|---|
| 结构/开模前综合评审 | 配置追溯、原生 CAD、结构/装配/DFM、风险与关闭要求 | [structure-review.md](references/structure-review.md) |
| 注塑件/注塑缺陷专项 | 局部厚度、筋柱、拔模、浇口/排气/冷却/包胶风险 | [injection-analysis.md](references/injection-analysis.md) |
| 受力/仿真/可靠性 | 用户选中的计算或测试策划模块 | [simulation-reliability.md](references/simulation-reliability.md) |
| 报告整理/修订 | 复用匹配版本的合格证据，只补必要缺口 | [report-delivery.md](references/report-delivery.md) |

涉及本机 CAD 操作时读取 [native-cad.md](references/native-cad.md)。不要默认加载所有专项，或将评审扩大成改图、供应商联系或量产签核。用户只要方案时，不执行 CAD 或仿真。

仅整理既有证据、摘要或检查清单时，核对文件来源、版本和数据一致性即可；不为流程完整而重开 CAD、重新测量或生成 Word/PDF。沿用的结果注明为既有记录；无法核实的来源关系保留缺口。

## 执行流程

### 1. 固定输入和当前状态

- 读取项目说明和已有成果，确认文件存在、版本、大小、修改时间和哈希。区分原始 CAD、转换副本、损坏恢复件、网格和旧报告。
- 独立任务目录通常包含 `source/`、`config/`、`evidence/`、`reports/`；使用仿真时再加 `simulation/`。延续任务复用匹配哈希的成果，不为目录名移动旧文件。
- 用 [source_guard.py](scripts/source_guard.py) 记录输入基线并在交付前比对；它只校验文件完整性，不证明 CAD 正确。
- 配置表保存来源、读取时间、工作表/版本、真实行列或单元格。每条要求关联零件、几何检查或测试项。单个按键的 ABS 标注不能推定整机都是 ABS；颜色不能证明软胶区域。
- 飞书优先使用当前可用的 lark sheets/wiki 能力，合法的已登录网页可作为备选。访问失败仍可继续 CAD 检查，但配置核对标记未完成；不把登录页或旧截屏当成配置全文。
- 只询问影响正确性的关键缺口；材料/模具/载荷缺失不阻塞所有独立工作，先完成不依赖它们的测量。

### 2. 核实原生几何

- 用户指定本机3D软件时，实际打开对应图档并取得原生视图；同一原生内核的脚本用于重复测量。优先已有软件/API/宏，不擅自订阅、付费或安装大型软件。
- 核实单位、坐标、全局位姿、层级、实例与有效性。保存新的审阅副本并复开；不覆盖原图或用户未保存文档。
- 叶节点与父装配聚合实体不能重复计数；真实重复实例不能仅因几何相同被删除。绑定源哈希、对象 ID、标签、1-based 面号及全局坐标，重导入后面号可能变化。
- 导入实体可以编辑/保存，不等于恢复原参数历史；网格、图片和生成图不替代精确制造几何。

### 3. 执行选中的工程模块

- 按功能和风险选检查对象，记录总范围、实际覆盖与排除项。允许没有问题，不预设缺陷数。
- 按上表读取专项。材料、模具或载荷不全时，完成 CAD 支持的部分，标明条件与不能产生的结论。
- 每个问题保留：编号、零件和位置、实测证据、适用条件、影响、建议动作、关闭证据、状态。区分几何事实、条件性风险、缺输入和未执行试验。
- 关键问题用剖面、交集实体、局部放大和尺寸标注定位；说明单位、方向和颜色。图应来自源 CAD 或真实求解器，不能用外观相似图充当证据。

### 4. 独立复核

- 对影响结论的尺寸、干涉、薄区及求解结果，另一路径计算或回到原生 CAD 测量。复制原结果再比较不算复核。
- 可并行安排配置、结构、注塑和独立审阅；文件所有权分开。主代理读取真实产物并核验关键结果，代理“完成”不等于验收。
- 崩溃、恢复提示、导出对话框、云保存标志都不证明成功；检查日志和真实输出再选择修复或替代路径，不盲目重跑。

### 5. 完成交付

- 按 [report-delivery.md](references/report-delivery.md) 组织报告与关闭清单；页数、图数和问题数随内容决定。
- 常用默认是可编辑 Word + PDF；用户指定格式优先。原生审阅副本与必要测量/参数保留在任务目录。
- 回读交付文件，渲染并逐页检查中文、表格、图片、图注、页码和链接；核对原始输入未变。
- 保存本轮范围、完成/未完成、复现方法与下一步。最终提供正式路径和影响使用的缺口，不能以 CAD、FEA 或渲染代替开模/量产批准。

## 输入基线工具

使用可用的 Python 3。`SKILL_DIR`、`TASK_DIR`、`CAD_FILE` 和 `CONFIG_SNAPSHOT` 由实际位置确定。宿主如要求 RTK 等命令前缀，遵循宿主规则。

```bash
python3 "$SKILL_DIR/scripts/source_guard.py" snapshot --manifest "$TASK_DIR/evidence/input_manifest.json" --files "$CAD_FILE" "$CONFIG_SNAPSHOT"
python3 "$SKILL_DIR/scripts/source_guard.py" verify --manifest "$TASK_DIR/evidence/input_manifest.json"
```

`snapshot` 拒绝覆盖已有清单；`verify` 对内容改变或文件缺失返回非零状态。用户提供修订版时建立新版本基线，不把合理修订伪报成损坏。
